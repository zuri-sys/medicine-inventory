import { useState } from 'react'; import { Link,useNavigate } from 'react-router-dom'; import { createMedicine } from '../services/medicineApi'
export default function AddMedicine(){const [data,setData]=useState({brand_name:'',category:'',stock_quantity:''}),[errors,setErrors]=useState({}),[apiError,setApiError]=useState('');const navigate=useNavigate();function submit(e){e.preventDefault();const x={};if(!data.brand_name.trim())x.brand_name='Brand Name is required.';if(!data.category.trim())x.category='Category is required.';if(data.stock_quantity==='')x.stock_quantity='Stock Quantity is required.';else if(!/^\d+$/.test(data.stock_quantity))x.stock_quantity='Stock Quantity must be a whole number.';setErrors(x);setApiError('');if(Object.keys(x).length)return;createMedicine({...data,stock_quantity:Number(data.stock_quantity)}).then(()=>navigate('/medicines')).catch(()=>setApiError('Unable to add medicine. Please check the form and try again.'))}return <main className="shell narrow"><header><div><p className="eyebrow">INVENTORY</p><h1>Add Medicine</h1></div><Link className="secondary button" to="/medicines">Cancel</Link></header><form className="card form" onSubmit={submit}>{[['brand_name','Brand Name','text'],['category','Category','text'],['stock_quantity','Stock Quantity','number']].map(([name,label,type])=><label key={name}>{label}<input type={type} value={data[name]} onChange={e=>setData({...data,[name]:e.target.value})} />{errors[name]&&<small className="error">{errors[name]}</small>}</label>)}{apiError&&<p className="message error">{apiError}</p>}<button>Add Medicine</button></form></main>}
